<div class="row">
	<div class="columns medium-12">
    <%= label_tag 'Cuota: ' %>
    <table>
      <thead>
        <tr>
          <th>Fecha Desde</th>
          <th>Monto</th>
          <th>Cobros</th>
          <th>Pendiente</th>
          <th>Pagos</th>
          <th>Pendiente</th>
        </tr>
      </thead>
      <tbody>
          <tr>
            <td><%= @liquidacion.contratos_item.fecha_desde %></td>
            <td><%= @liquidacion.contratos_item.monto %></td>
            <td><%= @liquidacion.contratos_item.get_cobrado %></td>
            <td><%= @liquidacion.contratos_item.get_a_cobrar %></td>
            <td><%= @liquidacion.contratos_item.get_pagado %></td>
            <td><%= @liquidacion.contratos_item.get_a_pagar %></td>
          </tr>
      </tbody>
    </table>
  </div>
</div>

